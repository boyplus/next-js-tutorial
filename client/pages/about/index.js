import Layout from '../../components/Layout';

const about = (props) => {
    return (
        <Layout>
            <h1>This is about page</h1>
            <p>This web application was created by Thanaphon Sombunakeo.</p>
        </Layout>
    );
};

export default about;
